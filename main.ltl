// ltl-query-planner/main.ltl
//
// Volcano-style query planner with rule-based optimisation.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-query-planner v0.1.0")
    io::println("Volcano-style query planner with rule-based optimisation.")
    0
}
